\begin{tikzpicture}
		\draw (0,0) node[ground] {}
		to[short, *-] ++ (-2,0)
		to[resistor, l=$R_2$] ++ (0,2) coordinate (A)
		to[resistor, l=$R_1$, *-] ++ (0,2)
		-- ++ (2,0)
		node[vcc] {$\sim U_{in}$} coordinate (B)
		to[short, *-] ++ (0,0)
		to[C, l=$C_1$, -*] ++ (0,-2)
		to[C, l=$C_2$, -*] ++ (0,-2)
		-- ++ (-1,0)
		(B) -- ++ (2,0)
		to[vC, l=$C_T$, -] ++ (0,-2) coordinate (C)
		to[short, -*] ++ (-2,0)
		(A) to[short] (C)
		to[short] (C)
		to[short, -o] ++ (1,0)
		node[auto, anchor=west] {$U_{out}$}
	;
\end{tikzpicture}
